name: Dow Weekly Report

on:
  schedule:
    # Saturday 18:00 Tehran = 14:30 UTC

    - cron: "30 14 * * 6"
    - cron: "45 14 * * 6"
    - cron: "0 15 * * 6"
    - cron: "15 15 * * 6"

  workflow_dispatch:
    inputs:
      force:
        description: "Bypass time gate"
        type: boolean
        default: false

      dry_run:
        description: "Run without sending Telegram"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: dow-weekly-report
  cancel-in-progress: false

jobs:
  report:

    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Dow weekly report
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL }}
          STATE_FILE_PATH: data/state_dow_weekly.json
          PYTHONPATH: src
        run: |
          python -m bot.main --asset dow --mode weekly \
            ${{ inputs.force && '--force' || '' }} \
            ${{ inputs.dry_run && '--dry-run' || '' }}

      - name: Commit updated state
        if: success() && inputs.dry_run != true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/state_dow_weekly.json

          if git diff --cached --quiet; then
            echo "No state changes."
            exit 0
          fi

          git commit -m "chore: update dow weekly state"

          git pull --rebase origin main

          git push
