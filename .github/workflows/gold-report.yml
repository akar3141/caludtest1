name: Gold Daily Report

on:
  schedule:
    # 09:00 America/New_York
    #
    # EST -> 14:00 UTC
    # EDT -> 13:00 UTC
    #
    # Each target has 4 attempts:
    # target, +15, +30, +45 minutes.

    - cron: "0 14 * * 1-5"
    - cron: "15 14 * * 1-5"
    - cron: "30 14 * * 1-5"
    - cron: "45 14 * * 1-5"

    - cron: "0 13 * * 1-5"
    - cron: "15 13 * * 1-5"
    - cron: "30 13 * * 1-5"
    - cron: "45 13 * * 1-5"

  workflow_dispatch:
    inputs:
      force:
        description: "Bypass time/trading-day gate"
        type: boolean
        default: false

      dry_run:
        description: "Run without sending Telegram"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: gold-daily-report
  cancel-in-progress: false

jobs:
  report:

    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Gold daily report
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL }}
          STATE_FILE_PATH: data/state_gold_daily.json
          PYTHONPATH: src
        run: |
          python -m bot.main --asset gold --mode daily \
            ${{ inputs.force && '--force' || '' }} \
            ${{ inputs.dry_run && '--dry-run' || '' }}

      - name: Commit updated state
        if: success() && inputs.dry_run != true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/state_gold_daily.json

          if git diff --cached --quiet; then
            echo "No state changes."
            exit 0
          fi

          git commit -m "chore: update gold daily state"

          git pull --rebase origin main

          git push
