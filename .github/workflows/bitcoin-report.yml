name: Bitcoin Daily Report

on:
  schedule:
    # 21:00 Asia/Tehran = 17:30 UTC; target +15/+30/+45
    - cron: "30 17 * * *"
    - cron: "45 17 * * *"
    - cron: "0 18 * * *"
    - cron: "15 18 * * *"

  workflow_dispatch:
    inputs:
      force:
        description: "Manual test only: bypass time/trading-day gate"
        type: boolean
        default: false
      dry_run:
        description: "Manual test only: run without sending Telegram"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: bitcoin-daily-report
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bitcoin daily report
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ vars.GEMINI_MODEL }}
          STATE_FILE_PATH: data/state_bitcoin_daily.json
          PYTHONPATH: src
        run: |
          CMD="python -m bot.main --asset bitcoin --mode daily"

          # Scheduled runs can NEVER bypass the Python time gate.
          # --force is available only for an explicit manual workflow_dispatch.
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.force }}" = "true" ]; then
            CMD="$CMD --force"
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.dry_run }}" = "true" ]; then
            CMD="$CMD --dry-run"
          fi

          echo "event=${{ github.event_name }}"
          echo "command=$CMD"
          $CMD

      - name: Commit updated state
        if: success() && (github.event_name != 'workflow_dispatch' || inputs.dry_run != true)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/state_bitcoin_daily.json

          if git diff --cached --quiet; then
            echo "No state changes."
            exit 0
          fi

          git commit -m "chore: update bitcoin daily state"
          git pull --rebase origin main
          git push
